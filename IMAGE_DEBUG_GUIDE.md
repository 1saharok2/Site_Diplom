# Диагностика проблемы с изображениями

## Проблема:
При переключении между вариантами товара (например, с 128GB на 256GB) изображения не загружаются и показывается "Загрузка изображения..."

## Что нужно проверить:

### 1. Откройте консоль браузера (F12)
На странице товара откройте консоль и посмотрите на логи:
- `🎨 ProductGallery - product:` - данные товара
- `🖼️ Final valid images:` - список изображений
- `❌ Image failed to load:` - ошибки загрузки

### 2. Проверьте структуру данных товара
В консоли должно быть видно:
```javascript
🎨 ProductGallery - product: {
  id: 39,
  name: "Смартфон Apple iPhone 16 256GB Синий",
  images: ["/images/products/Phones/Apple/Iphone 16 blue/iphone_16_blue (2).jpg", ...],
  // или
  image_url: ["/images/products/Phones/Apple/Iphone 16 blue/iphone_16_blue (2).jpg", ...]
}
```

### 3. Проверьте доступность изображений
В консоли браузера выполните:
```javascript
// Тест доступности изображений
function testImageUrls() {
  const testUrls = [
    'https://electronic.tw1.ru/images/products/Phones/Apple/Iphone 16 blue/iphone_16_blue (2).jpg',
    'https://electronic.tw1.ru/images/products/Phones/Apple/Iphone 16 blue/iphone_16_blue (3).jpg',
    'https://electronic.tw1.ru/images/products/Phones/Apple/Iphone 16 blue/iphone_16_blue (4).jpg'
  ];
  
  console.log('🔍 Тестирование доступности изображений...');
  
  testUrls.forEach((url, index) => {
    const img = new Image();
    img.onload = () => {
      console.log(`✅ Изображение ${index + 1} загружено: ${url}`);
    };
    img.onerror = () => {
      console.log(`❌ Изображение ${index + 1} не загружено: ${url}`);
    };
    img.src = url;
  });
}

testImageUrls();
```

### 4. Проверьте структуру папок на сервере
Убедитесь, что на сервере есть папки:
```
/images/products/Phones/Apple/Iphone 16 blue/
/images/products/Phones/Apple/Iphine 16 pink/
/images/products/Phones/Apple/Iphone 16 [другие цвета]/
```

### 5. Проверьте права доступа
Убедитесь, что веб-сервер имеет права на чтение файлов изображений.

## Возможные причины проблемы:

1. **Неправильные пути к изображениям** - файлы не существуют по указанным путям
2. **Пробелы в именах файлов** - могут вызывать проблемы с URL
3. **Права доступа** - веб-сервер не может прочитать файлы
4. **Кеширование** - браузер кеширует старые данные

## Что нужно предоставить:

1. **Логи из консоли браузера** при переключении между вариантами
2. **Структуру папок** с изображениями на сервере
3. **Пример данных товара** из API (как выглядят поля images/image_url)
4. **Результат теста** доступности изображений

## Временное решение:

Если проблема в путях к изображениям, можно:
1. Переименовать папки без пробелов
2. Использовать URL-кодирование для пробелов
3. Создать символические ссылки с правильными именами
